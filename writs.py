class Writs:
        def __init__(self, style, wtype, trait, qual, Set):
                self.style = style
                self.wtype = wtype
                self.trait = trait
                self.qual = qual
                self.Set = Set

